# Plotting
pip install plotly

# Python portus package for our CCP algs
pip install portus

# Fix base repo mahimahi graph (mm-graph) issues, can remove if not needed
apt-get install gnuplot
sudo apt-get install texlive-font-utils